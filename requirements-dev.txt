# Everything in the runtime, plus the training pipeline and the test suite.
#
#   pip install -r requirements-dev.txt
#
# Needed to run backend.ml.* (download, build_dataset, train, export_models,
# build_serving_data) and to run the tests. Not deployed.

-r requirements.txt

# Modelling
scikit-learn>=1.4.0
pandas>=2.0.0
joblib>=1.3.0
pyarrow>=15.0.0        # parquet, for the processed ball-by-ball tables

# Tests
pytest>=8.0.0
httpx2>=0.1.0          # required by fastapi.testclient
