# Runtime dependencies. This is what gets deployed, and it is deliberately
# short: the fitted models are exported to JSON and npz by
# backend/ml/export_models.py, and backend/app/inference.py evaluates them in
# NumPy alone.
#
# Adding scikit-learn, SciPy, pandas or pyarrow here would take the bundle
# from ~71 MB to ~415 MB and break deployment to any serverless platform.
# tests/test_runtime_weight.py fails if one of them is imported while serving.
#
# To retrain or rebuild the data, install requirements-dev.txt as well.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
numpy>=1.26.0
